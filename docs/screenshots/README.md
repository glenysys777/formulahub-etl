# Screenshot placeholders for README
#
# Capture and drop files here:
#   canvas-demo.png   — React Flow canvas with S3→Snowflake demo
#   ai-builder.png    — English prompt → generated graph
#   run-metrics.png   — sidebar metrics + logs after a successful run
