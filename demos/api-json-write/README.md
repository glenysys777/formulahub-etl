# API → Schema from JSON → Write JSON (founder Soft-PASS)

Pipeline id: **`demo-api-json-write`**

## Open in Studio

1. Start API + Studio (`make api` / desktop app).
2. Click **JSON write demo** in the top bar  
   — or open Studio with `?pipeline=demo-api-json-write`.
3. Click **Run**. Watch **rows in → rows out** badges tick and edges flow before completion.
4. Inspect `data/out/api_orders.json` (nested `order` / `customer`).

See `docs/sales/FOUNDER_WIN_2027.md` for sell language (DEMO Soft-PASS vs LIVE).
