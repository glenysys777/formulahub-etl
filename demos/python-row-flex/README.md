# Python Row Flex

**Python Row** — custom per-row or batch logic in a restricted Python sandbox.

Use when a formula or Field Mapper expression is not enough: normalize values, derive columns, or apply business rules in Python.
